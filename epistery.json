{
  "name": "epistery/files",
  "title": "Files",
  "version": "0.3.0",
  "description": "File management with Storj storage and data wallet origin tracking",
  "author": "Epistery",
  "main": "index.mjs",
  "icon": "/agent/epistery/files/icon.svg",
  "widget": "/agent/epistery/files/widget",
  "noUserInterface": false,
  "config": {
    "uploadList": "files::upload",
    "manageList": "files::manage"
  },
  "tools": [
    {
      "name": "files_list",
      "scope": "files:read",
      "description": "List files and folders in a directory. Returns arrays of files and folders.",
      "method": "GET",
      "path": "/api/list",
      "inputSchema": {
        "type": "object",
        "properties": {
          "folder": { "type": "string", "description": "Folder path to list. Empty string or omitted for root." }
        }
      }
    },
    {
      "name": "files_metadata",
      "scope": "files:read",
      "description": "Get file metadata by ID. Returns name, size, type, hash, upload info.",
      "method": "GET",
      "path": "/api/file/{id}/meta",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "description": "File ID (hex string)" }
        },
        "required": ["id"]
      }
    },
    {
      "name": "files_read",
      "scope": "files:read",
      "description": "Read file contents by ID. Returns { encoding, content } where content is utf-8 text for text/json/xml mimetypes and base64 otherwise. Capped at 25 MB; use files_download for larger files.",
      "method": "GET",
      "path": "/api/file/{id}/content",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "description": "File ID (hex string)" },
          "encoding": { "type": "string", "description": "Force return encoding. Auto-selected if omitted.", "enum": ["text", "base64"] }
        },
        "required": ["id"]
      }
    },
    {
      "name": "files_write",
      "scope": "files:write",
      "description": "Create a new file from inline content. Requires editor access.",
      "method": "POST",
      "path": "/api/file",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "File name including extension" },
          "folder": { "type": "string", "description": "Target folder path. Empty string or omitted for root." },
          "content": { "type": "string", "description": "File content (utf-8 text, or base64-encoded bytes when encoding=base64)" },
          "mimetype": { "type": "string", "description": "MIME type. Defaults to text/plain or application/octet-stream based on encoding." },
          "encoding": { "type": "string", "description": "Content encoding. Defaults to text.", "enum": ["text", "base64"] }
        },
        "required": ["name", "content"]
      }
    },
    {
      "name": "files_create_folder",
      "scope": "files:write",
      "description": "Create a new folder. Requires editor access.",
      "method": "POST",
      "path": "/api/folder",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "Folder name (letters, numbers, hyphens, underscores only)" },
          "parentFolder": { "type": "string", "description": "Parent folder path. Empty string or omitted for root." }
        },
        "required": ["name"]
      }
    },
    {
      "name": "files_rename",
      "scope": "files:write",
      "description": "Rename a file. Requires owner or admin access.",
      "method": "PATCH",
      "path": "/api/file/{id}",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "description": "File ID (hex string)" },
          "newName": { "type": "string", "description": "New file name" }
        },
        "required": ["id", "newName"]
      }
    },
    {
      "name": "files_delete",
      "scope": "files:write",
      "description": "Delete a file. Requires owner or admin access.",
      "method": "DELETE",
      "path": "/api/file/{id}",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "description": "File ID (hex string)" }
        },
        "required": ["id"]
      }
    }
  ]
}
